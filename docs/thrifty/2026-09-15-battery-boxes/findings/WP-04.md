| WP | Where | What was found | Evidence | Suggested owner |
|----|-------|----------------|----------|-----------------|
| 4 | cad/params.toml [motor] | `axis` was "Y" (cross-vehicle); owner's notebook sketch and the LT-A400F prop-shaft/diff reference photos show the motor in-line, fore-aft, parallel to the prop-shaft stack. Corrected to "X". | Session transcript 2026-09-16; owner-supplied photos of motor mount sketch and Suzuki service manual pages | Owner - confirm before WP1-WP3 artefacts (which assumed axis=Y) are re-derived |
| 4 | cad/params.toml [motor].centre | Default [320,0,450] buried in the frame (21452 points inside, min distance 0.009mm). Iteratively repositioned via owner review against a live three.js viewer to [252,0,590], behind the battery boxes, clear of the rear shock by inspection. Not scan-verified at the final position. | `docs/thrifty/2026-09-15-battery-boxes/artefacts/box-check-viewer.html`; session transcript 2026-09-17 | Owner - verify chain/belt centre distance to the intermediate drive once its own position is set |
| 4 | cad/assembly.py transform (section 4.6) | Owner's actual design intent for box lean is a **hinge rotation about each box's inner-most edge** (the longest edge, parallel to X, at local Y=-outer_y/2, Z=-sheet), not the origin-based rotation assembly.py implements. The committed params.toml [placement] x/y/z/lean_deg for 2026-09-17 are back-solved equivalents that reproduce the correct hinge position (Y=+-15mm, Z=591.57mm, 30mm gap) through the existing origin-based transform - verified exactly via assembly.py's own inboard_gap_at_floor (30.000mm). If the hinge position needs to move independently of x/lean_deg in future (e.g. gap and tilt set separately without re-solving), assembly.py's transform needs a real hinge-pivot implementation. | Session transcript 2026-09-17; params.toml [placement] comments | Owner/next WP - decide whether to formalise hinge-pivot as the section 4.6 transform |
| 4 | cad/params.toml [placement] | Owner has deprioritised scan-collision verification for the current placement in favour of getting real component CAD (motor, intermediate drive) into the model. The committed x/y/z/lean_deg is NOT confirmed clear of the frame - last checked collision at a nearby z showed left 14985 / right 41087 frame points inside the boxes. | Session transcript 2026-09-17 | Owner - re-run the WP-04 sweep once real components are modelled and placement is closer to final |
| 4 | cad/clearance.py --sweep, 2026-09-17 | Full sweep (525 placements, grid re-centred on the current placement) found zero passing points. The least-bad point still has 15580 frame points inside the box and min_frame=0.56mm. The current committed [placement] does not clear the frame anywhere nearby in this grid. | `docs/thrifty/2026-09-15-battery-boxes/artefacts/sweep.json`, `sweep-report.md`, `section-*-best.png` | Owner - this is a real placement/lean problem, not a script defect; needs a design decision |
