| WP | Where | What was found | Evidence | Suggested owner |
|----|-------|----------------|----------|-----------------|
| 2 | plan.md section 4.5, "front column needs a spacer of `p - partition`" | The front column's last cell is already flush with the 9-cell columns' last cell (both placed by `y0 + p*slot` at slot = n_max - 1, confirmed against section 4.4's own 24.06 mm travel figure). Computed gap is 0.0 mm, not `p - partition` (26.66 mm). Building a block of that literal thickness overlaps the compression plate, which spans the full cavity width. No spacer solid was built; `box.step` has 7 solids (not 8), `box-full.step` has 33 (not 34). | `cad/enclosure.py:front_column_spacer()`; run output `front-column spacer: gap = 0.0 mm`; `enclosure-report.md` section "Front-column spacer" | Owner: confirm whether section 4.4/4.5's cell layout or the spacer requirement itself needs revising |
| 2 | params.toml `[electrical].fuse_envelope` (OWNER: confirm) | `fuse_envelope` Y = 45 mm is wider than the fuse bay's own Y span (p = 29.66 mm) and wider than the space actually free between the end plate and the partition (p - partition = 26.66 mm). Centred at the bay midpoint the envelope pushes through the body wall; clamped inside the cavity it still overlaps the end plate, the partition and the block's first real cell (32 400-50 436 mm3 per pair). | `cad/enclosure.py:fuse_envelopes()`; `enclosure-report.md` interference table rows `end_plate/fuse_*`, `partition/fuse_*`, `fuse_*/block` | Owner: confirm the real fuse-holder footprint from the datasheet/holder in hand, per section 5.7 |
| 2 | params.toml `[enclosure].chamfer_front_top` vs `sheet` | A 45 deg chamfer of leg 20 mm (the value section 4.5's WP-02 prompt names to prove the chamfer branch builds) does not fit a 3 mm sheet: OCCT raises `BRep_API: command not done`. The branch was proven instead at leg = 2.0 mm, which builds and removes ~612 mm3 as expected for that edge length. The committed `chamfer_front_top = 0.0` is unaffected. | Run output: leg 20 traceback, leg 2.0 succeeds (`cad/enclosure.py:apply_front_top_chamfer()`) | Owner: any future non-zero `chamfer_front_top` must stay within a few mm given `sheet = 3.0` |
