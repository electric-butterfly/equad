---
wp: 4
plan: 2026-09-15-battery-boxes
state: paused
model: sonnet
effort: medium
branch: thrifty/2026-09-15-battery-boxes/wp-04
progress: 4/11
resume-from: step-5-full-sweep
pause-reason: owner
updated: 2026-09-17
---

## Done-when evidence
`.venv-mesh/Scripts/python cad/clearance.py --controls` (canary, item 4) passed both control
rows against the plan section 4.7 expected values:
- collision (713, 300, 330, -30): inside_frame 198712, min_frame 0.014mm (expect inside > 50000,
  min_frame < 1.0) - PASS
- clear (700, 300, 1500, 30): inside_frame 0, min_frame 225.19mm (expect inside = 0,
  min_frame > 200) - PASS

Items 5-11 (the full sweep, summary, section renders, sweep-report.md) were not run: the owner
redirected the session to live placement iteration (motor position/orientation, box lean and
gap) using an ad hoc three.js viewer instead, and explicitly deprioritised collision-sweep
precision in favour of getting real component geometry into the model. See findings/WP-04.md
rows 4-7 for what changed and why.

## Notes
`cad/params.toml` now carries several owner-directed corrections beyond this WP's original
scope, made live against `docs/thrifty/2026-09-15-battery-boxes/artefacts/box-check-viewer.html`
(a standalone three.js viewer, not a plan deliverable - built because flat 2D renders proved
unreliable for judging real clearance/orientation):

- `[motor].axis` corrected Y -> X (in-line with the prop shafts, not cross-vehicle)
- `[motor].centre` moved from the buried default to [252, 0, 590]
- `[placement]` x/y/z/lean_deg changed several times; the final committed values are
  back-solved equivalents reproducing the owner's actual reference (a hinge rotation about
  each box's inner-most edge, 30mm gap, 8deg off vertical) through `assembly.py`'s existing
  origin-based transform. Verified exactly via `assembly.py`'s own `inboard_gap_at_floor`
  (30.000mm) after regenerating `assembly.step`/`placement.json` through the real pipeline.

**Not done, and no longer this session's job to finish without new direction:**
- The full 1701-point sweep (item 5), sweep.json, sweep-report.md, and the six section PNGs
  (items 6-11) per plan section 4.7.
- Scan-collision verification of the final placement - last checked (at a nearby z) it still
  collides (left 14985 / right 41087 frame points inside).
- `assembly.py` still only implements origin-based rotation. If the hinge position needs to
  move independently of tilt in future, that transform needs a real code change (findings
  row 4 has the detail) - this is a plan-level (section 4.6) decision, not something to patch
  ad hoc.

Resuming this WP means either (a) finishing the original sweep now that a placement exists, or
(b) waiting until real component CAD (motor, intermediate drive) is modelled, per the owner's
stated preference in the session that produced this pause.
