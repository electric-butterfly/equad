---
wp: 4
plan: 2026-09-15-battery-boxes
state: done
model: sonnet
effort: medium
branch: thrifty/2026-09-15-battery-boxes/wp-04
progress: 11/11
updated: 2026-09-17
---

## Done-when evidence
`.venv-mesh/Scripts/python cad/clearance.py --controls` (canary) passed both control rows:
- collision (713, 300, 330, -30): inside_frame 198712, min_frame 0.014mm - PASS
- clear (700, 300, 1500, 30): inside_frame 0, min_frame 225.19mm - PASS

`.venv-mesh/Scripts/python cad/clearance.py --sweep` ran the full grid and wrote sweep.json:
```
python -c "import json; r=json.load(open('...sweep.json')); print(len(r), sum(1 for x in r if x['pass']))"
-> 525 0
```
`head -c 200 sweep-report.md` begins "This sweep tests the frame..." as required.
`ls artefacts/section-*.png` -> six files, all under 500kB (largest 188494 bytes).

**Deviation from the plan's literal verify numbers**: the plan expected 1701 rows and a pass
count greater than 0. This sweep has 525 rows because the `[sweep]` grid was re-centred on the
owner's 2026-09-17 placement (x~743, y~39, z~440, lean~82) - the original grid (lean 0-40,
y 200-400) predates that correction and does not bracket the current design point at all. The
owner directed resuming the sweep "now that placement exists"; re-centring the grid was
necessary to test the placement that actually exists. 0 passes is a real, verified result, not
a partial run - see findings row 8.

## Notes
Everything from the previous pause still applies (motor axis correction, hinge-rotation
back-solve, `assembly.py` still origin-based only - see the prior version of this file in git
history, or findings/WP-04.md rows 1-7).

**New this session**: the full sweep ran (525 placements, re-centred grid) and found **zero
passing placements**. Every point tested has real frame points inside the box - the best
(least-bad) point found still has 15580 frame points inside and min_frame=0.56mm, not a fit.
`sweep-report.md` names it and the `section-*-best.png` renders show it. The committed
`[placement]` defaults are confirmed NOT clear of the frame at any nearby point in this grid.

This WP's job (run the sweep, report it) is done. What it found is a real geometry problem: the
current box placement/lean does not fit anywhere near itself in this frame region. That is an
owner decision to resolve (different placement, different lean, or accept the "real components"
workflow the owner already redirected toward), not something this WP script can fix.
