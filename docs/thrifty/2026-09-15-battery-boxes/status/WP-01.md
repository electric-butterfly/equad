---
wp: 1
plan: 2026-09-15-battery-boxes
state: blocked
model: sonnet
effort: low
branch: thrifty/2026-09-15-battery-boxes/wp-01
progress: 0/9
resume-from: precondition-3
pause-reason: missing-tool
updated: 2026-09-15
---

## Done-when evidence

Precondition 1 (right tree): `git rev-parse --show-toplevel` -> `C:/SANDPIT/equad`;
`git remote get-url origin` -> `https://github.com/electric-butterfly/equad.git`. Pass.

Precondition 2 (plan present): `docs/thrifty/2026-09-15-battery-boxes/plan.md` exists. Pass.

Precondition 3 (tools): `pipeline/config.local.toml` exists but `freecad_cmd = ""` — the
key is present with an empty value, not a usable path. No FreeCAD installation found on
this machine (`where freecadcmd`, `where FreeCADCmd`, and a search of
`C:/Program Files`, `C:/Program Files (x86)` for `*freecad*` all returned nothing).
`.venv-cad/Scripts/python -c "import build123d"` succeeds, build123d 0.11.1. Fail: no
`freecad_cmd`.

## Notes

Per the WP-01 starter prompt precondition 3: "If config.local.toml is missing, write
status with state: blocked ... and STOP." The file is present but its `freecad_cmd` value
is empty, which is functionally the same blocker — `common.py`'s `export_step`/`check_step`
cannot run the FreeCAD round-trip check without it, and section 4.9's `freecad_check` is
required for every STEP export in this WP (items 4 and 6).

To unblock: install FreeCAD (1.1.3, per plan section 5.3) and set `freecad_cmd` in
`pipeline/config.local.toml` to `freecadcmd.exe`'s path. Then resume this WP from
precondition 3.

No files were written outside this status file. No CAD scripts were created or run.
