---
wp: 1
plan: 2026-09-15-battery-boxes
state: done
model: sonnet
effort: low
branch: thrifty/2026-09-15-battery-boxes/wp-01
progress: 9/9
resume-from: complete
updated: 2026-09-16
---

## Done-when evidence

`.venv-cad/Scripts/python cad/cell.py --report`:

```
cell volume: 406850.0799453903 mm3
cell bbox: 148.24 x 26.66 x 105.9
freecad: 1 solids, 406850.0799453903 mm3 (build123d: 1 solids, 406850.0799453903 mm3) OK
block bbox: 452.24 x 263.94 x 105.9, 26 cells
fit rule: stack=263.94, needed=285.94, internal_y=300.0, travel=24.060000000000002
freecad: 26 solids, 10578102.07858013 mm3 (build123d: 26 solids, 10578102.078580149 mm3) OK
```

Cell volume matches 406 850 mm3 within 10 mm3. Block bounding box matches
452.24 x 263.94 x 105.9 exactly. Both STEP round trips (N=1, N=26) pass through FreeCAD
with build123d, volumes identical.

`cell-report.md` exists with the parameter table and the 26-row position table.

`git status --short` shows only `cad/` and
`docs/thrifty/2026-09-15-battery-boxes/artefacts/` (new): `cad/params.toml`,
`cad/common.py`, `cad/cell.py`, `cad/README.md`, `artefacts/cell.step`,
`artefacts/block.step`, `artefacts/cell-report.md`, plus this status file. `cad/__pycache__/`
is git-ignored.

Pad sensitivity, `block()` called directly against scratch copies of `params.toml`
(`cad/params.toml` itself left at pad = 3.0):

- pad = 1.0: Y span 247.94, stack 247.94, travel 40.06 — matches plan section 4.4
  ("stack is 248 and the travel is 40").
- pad = 5.0: fit rule fails as designed — `end_plate 6.0 + stack 279.94 +
  compression_plate 6.0 + travel_min 10.0 = 301.94 > internal_y 300.0`, script prints
  the four numbers and exits 2. The plan's section 4.4 only asserts pad = 1.0 and
  pad = 3.0 pass; it makes no claim about pad = 5.0, so this exit is the fit rule working
  as specified, not a defect.

## Notes

Precondition 3 was blocked in the prior session (`freecad_cmd` empty, no FreeCAD
installed). The owner installed FreeCAD 1.1.3 and this session set
`freecad_cmd = "C:/Program Files/FreeCAD 1.1/bin/freecadcmd.exe"` in
`pipeline/config.local.toml` (git-ignored, not committed) before resuming on the
existing `thrifty/2026-09-15-battery-boxes/wp-01` branch.

`cell.py`'s `Cylinder(..., align=(0, 0, 0))` from the initial draft was wrong — build123d's
`align` takes `Align` enum members, not integers. Fixed to
`(Align.CENTER, Align.CENTER, Align.MIN)` before the canary ran; the canary output above is
from the corrected script.

All nine items complete. Ready for review and merge; WP-02 (enclosure) depends on this
PR merging first.
