---
wp: 5
plan: 2026-09-15-battery-boxes
state: done
model: sonnet
effort: medium
branch: thrifty/2026-09-15-battery-boxes/wp-05
progress: 7/7
updated: 2026-09-17
---

## Done-when evidence

Precondition 3 (inputs) check:
```
git log -1 --format=%ct -- cad/params.toml               -> 1789613425
git log -1 --format=%ct -- docs/thrifty/2026-09-15-battery-boxes/artefacts/sweep-report.md
                                                            -> 1789613425
```
Equal, not params-time-greater: both were last touched by the same commit
(`5e5e8c33`, "WP-04 done - sweep re-run against corrected placement, 0/525 pass").
The ordering check the plan specifies does not pass.

The substantive reason is more than a timestamp technicality. Per
`docs/thrifty/2026-09-15-battery-boxes/status/WP-04.md` and
`docs/thrifty/2026-09-15-battery-boxes/findings/WP-04.md` (row 8), the WP-04 sweep
re-run against the current `[placement]` (x=743.2, y=39.2643, z=440.4765,
lean_deg=82.0) found **zero passing placements out of 525**. The least-bad point
still has 15,580 frame points inside the box and `min_frame` = 0.56 mm — the
committed placement is confirmed not clear of the frame anywhere nearby.

## Done-when evidence

```
.venv-mesh/Scripts/python cad/mounts.py
-> candidate primitives: 173 accepted cylinders, 48 planes (support > 1500)
-> kept (within 120 mm): 104
-> wrote artefacts/mount-pickups.json (51150 bytes)
-> wrote artefacts/mounts-report.md (9674 bytes)

python -c "import json; r=json.load(open('...mount-pickups.json')); \
  print(len(r), all(r[i]['distance']<=r[i+1]['distance'] for i in range(len(r)-1)), \
  all(x.get('note')=='calliper before drilling' for x in r if x['kind']=='plane'))"
-> 104 True True

grep -c "calliper before drilling" artefacts/mounts-report.md
-> 38
```

## Notes

Owner directed, in-session 2026-09-17: the current `[placement]` is correct for now
— chassis modifications and minor adjustments to placement are expected later. WP-05
proceeded against the committed `[placement]` (743.2, 39.2643, 440.4765, lean 82°)
regardless of the WP-04 sweep result (0/525 pass). PR #18 (opened while blocked) was
closed, superseded, rather than reused, per the plan's "never add commits to a PR
that is already open" rule.

`.venv-cad` did not exist in this worktree (git-ignored, not shared across
worktrees) and was rebuilt: `python -m venv .venv-cad` then
`.venv-cad/Scripts/python -m pip install -r pipeline/requirements-cad.txt`;
`import build123d` confirmed version 0.11.1.

Nearest-face labelling: `cad/mounts.py` transforms each candidate primitive's point
into the box-local frame (inverse of the section 4.6 rigid transform, including the
right-box mirror) and picks the face whose outward offset is largest — exact for an
axis-aligned box (the current `chamfer_front_top = 0`), and reasonable near a corner
otherwise. Distance itself comes from an Open3D RaycastingScene on the placed
`box-shell.stl`, not the analytic box, so it stays correct if a chamfer is ever
enabled.

`cad/mounts.py` imports `place_vertices`, `get_shell_mesh` and `scene_from_mesh` from
`cad/clearance.py` (WP-04) rather than re-deriving the section 4.6 transform.
