---
wp: 6
plan: 2026-09-15-battery-boxes
state: in-progress
model: sonnet
effort: medium
branch: thrifty/2026-09-15-battery-boxes/wp-06
progress: 4/10
resume-from: item-5
updated: 2026-09-17
---

## Done-when evidence

Canary (item 1, .venv-mesh decimation):
```
face count: 60000
soup bytes: 2160000
base64 length: 2880000
```

Items 2-4 (.venv-cad tessellation of assembly.step, cad/viewer.py, viewer.html):
```
.venv-cad/Scripts/python cad/viewer.py
chassis: 60000 faces, base64 2880000 chars
box_left: base64 576 chars
box_right: base64 576 chars
motor: base64 24000 chars
keepout: base64 24000 chars
wrote .../artefacts/viewer.html (2935648 bytes)
wrote .../artefacts/sam-battery-boxes.html (12026 bytes)
```
(sam-battery-boxes.html was written this run too; items 5-10 finish and check it.)

viewer.html checks:
```
ls -la viewer.html      -> 2935648 bytes (under 8 000 000)
grep -o "https://..."   -> 1 match (three.js r128 on cdnjs)
grep -c '": "'          -> 5 (chassis, box_left, box_right, motor, keepout)
```

## Notes

assembly.step's four solids come back from build123d's import_step in Compound child order
(box_left, box_right, motor, keepout); viewer.py asserts this via each solid's bounding-box
centre (box_left centre.Y > 0, box_right centre.Y < 0) before using positional order, so a
future change to assembly.py's child order fails loudly instead of silently mislabelling a body.

cad/viewer.py runs under .venv-cad (build123d) per cad/README.md, but the chassis decimation
needs open3d, which only exists in .venv-mesh. viewer.py shells out to
`.venv-mesh/Scripts/python.exe` for that one step via subprocess and caches the result at
data/cad/chassis_60k.b64 (git-ignored) rather than reading two venvs into one process.
