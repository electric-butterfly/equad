---
wp: 2
plan: 2026-09-14-scan-pipeline
state: done
model: sonnet
effort: low
branch: thrifty/2026-09-14-scan-pipeline/wp-02
progress: 8/8
resume-from: —
updated: 2026-09-14
---

## Done-when evidence

- `.venv-cad`: `build123d==0.11.1`, resolved `cadquery-ocp-novtk==7.9.3.1.1` — matches
  plan section 5.6's dry-run prediction.
- `.venv-draft`: `draftwright==0.4.29`, resolved `build123d==0.10.0`,
  `cadquery-ocp==7.8.1.1.post1`, `vtk==9.3.1` — matches plan section 5.6.
- FreeCAD 1.1.3 installed via winget (per-user, under `%LOCALAPPDATA%\Programs\FreeCAD 1.1\`,
  not `%ProgramFiles%` as the prompt assumed).
- FreeCAD_SheetMetal cloned into `%APPDATA%\FreeCAD\v1-1\Mod\SheetMetal` at commit
  `d99e89ada00c9de85243878536c52ede1bcb8953`.
- Smoke build123d: 10x20x30 mm box round-tripped through STEP, `volume 6000.0`.
- Smoke draftwright: `draftwright.exe data/smoke_box.step --out-dir data` produced a
  59258-byte PDF.
- Smoke FreeCAD: `freecadcmd -c "import Part, SheetMetalNewUnfolder; print('ok')"` ->
  `ok`, after installing `networkx==3.6.1` into FreeCAD's bundled Python (see findings).
- Full details and verbatim outputs in `artefacts/toolsets-cad.md`.

## Notes

Working-tree hazard: this session shared a single checkout with a concurrent WP-01
session. Partway through, HEAD was switched to `wp-01` by that other session and a
duplicate of its commit landed on this branch's local ref (never pushed). Fixed by
resetting local `wp-02` to `origin/wp-02` before committing this WP's own work — no
WP-01 content was lost (it is safely committed and pushed on the real `wp-01` branch).
Flagged to the owner; recommend future concurrent WPs use separate `git worktree`
checkouts, not one shared working directory.
