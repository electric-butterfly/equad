---
wp: 1
plan: 2026-09-14-scan-pipeline
state: done
model: sonnet
effort: low
branch: thrifty/2026-09-14-scan-pipeline/wp-01
progress: 9/9
resume-from: complete
updated: 2026-09-14
---

## Done-when evidence

`.venv-mesh/Scripts/python -c "import pymeshlab, open3d, trimesh, pyransac3d; print(open3d.__version__, trimesh.__version__)"`
```
0.19.0 5.1.0
```

Smoke (b) synthetic 12.5 mm cylinder through CloudCompare RANSAC: measured radius 12.550 mm
(expect 12.0-13.0).

Smoke (c) pymeshlab sphere decimate/reload: face count 500 (expect 500).

`git status --short --untracked-files=all` after the batch: only `.gitignore`,
`docs/thrifty/2026-09-14-scan-pipeline/artefacts/toolsets-mesh.md`, `pipeline/README.md`,
`pipeline/common.py`, `pipeline/config.example.toml`, `pipeline/requirements-mesh.txt`.
`.venv-mesh/` and `data/` do not appear — `.gitignore` is working.

## Notes

Two out-of-band observations during this session, both written to
`docs/thrifty/2026-09-14-scan-pipeline/findings/WP-01.md`:

1. The status file was reset to a minimal `state: not-started` stub mid-batch, overwriting the
   `started` state this session wrote. Not this session's edit.
2. `pipeline/requirements-cad.txt` (WP-02's file, `build123d==0.11.1`) appeared in the working
   tree during this session's batch, timestamped in the same minute as this session's own writes.

Both were caused by a concurrent WP-02 session sharing this same working directory rather than an
isolated worktree/clone: the reflog showed this session's own checkout switched from `wp-01` to
`wp-02` mid-session, and this session's final commit landed on `wp-02` on top of WP-02's own start
commit before being caught, pushed, then corrected by cherry-picking onto `wp-01` and force-pushing
`wp-02` back to its own start commit. Flagged to and confirmed with the owner in-session.

CloudCompare was already installed on this machine (winget reported "no upgrade available");
version not re-confirmed via `--version` this session. MeshLab installed fresh at 2025.07.
