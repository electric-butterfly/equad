| WP | Where | What was found | Evidence | Suggested owner |
|----|-------|----------------|----------|-----------------|
| 2 | winget FreeCAD.FreeCAD install | Installs per-user under `%LOCALAPPDATA%\Programs\FreeCAD 1.1\`, not `%ProgramFiles%` as the plan's WP-02 prompt assumed | `winget list --id FreeCAD.FreeCAD` shows "Installed for Current User"; `freecadcmd.exe` found under `%LOCALAPPDATA%\Programs\FreeCAD 1.1\bin\` | owner (plan text, if reused elsewhere) |
| 2 | FreeCAD_SheetMetal / `SheetMetalNewUnfolder.py` | Imports `networkx` unconditionally past a soft-fail guard; the workbench does not vendor it, so a fresh FreeCAD install fails the smoke import until it is installed manually into FreeCAD's bundled Python | `import Part, SheetMetalNewUnfolder` raised `NameError: name 'nx' is not defined` until `networkx==3.6.1` was pip-installed into `%LOCALAPPDATA%\Programs\FreeCAD 1.1\bin\python.exe` | owner (upstream issue, or document as a required manual step) |
| 2 | Shared working tree | A concurrent WP-01 session shared this same git checkout; HEAD was switched underneath this session mid-run and a duplicate commit landed on the local `wp-02` branch ref (never pushed, no data lost — fixed by resetting to `origin/wp-02`) | `git log --oneline --graph` on both branches; `git ls-remote --heads origin` showing `origin/wp-02` unaffected | owner — recommend `git worktree` per concurrent WP, not one shared directory |
