| WP | Where | What was found | Evidence | Suggested owner |
|----|-------|----------------|----------|-----------------|
| 1 | `docs/thrifty/2026-09-14-scan-pipeline/status/WP-01.md` | Status file was overwritten mid-session to a minimal `state: not-started` stub, replacing the `started` state this session had just committed and pushed. Not this session's edit. | Session tooling surfaced the on-disk change with only `wp`, `plan`, `state: not-started` in front matter. | Owner — check for a concurrent process/session writing to this same checkout. |
| 1 | `pipeline/requirements-cad.txt` | File containing `build123d==0.11.1` (WP-02's deliverable) appeared in the working tree unrequested, timestamped 2026-09-18 22:24, the same minute as this session's own item-8/9 writes. | `git status --short --untracked-files=all` showed it; `Get-ChildItem`/`cat` confirmed content and mtime. | Owner — same likely cause as the row above: another WP-02 session sharing this working directory instead of its own clone/worktree, risking branch crossover. |
