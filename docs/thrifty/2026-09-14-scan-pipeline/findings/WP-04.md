| WP | Where | What was found | Evidence | Suggested owner |
|----|-------|----------------|----------|-----------------|
| 4 | `.venv-mesh` (main checkout at repo root, and this worktree) | Missing `pyvenv.cfg` — the venv is not usable as a Python interpreter from these locations | `python.exe -c "import open3d"` failed with "No pyvenv.cfg file"; only the `wp3-start-4b3e7c` worktree's `.venv-mesh` has a working `pyvenv.cfg` and was used instead for this WP's runs | owner |
